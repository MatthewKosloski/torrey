package me.mtk.torrey.symbols;

/**
 * Various categories of different types of symbols. 
 * 
 * Right now, we only have variables. If we were to add 
 * functions to the language, then we would add another
 * category called "FUNCTION".
 */
public enum SymCategory 
{
    VARIABLE
}
